tag: v0.6.3

infrastructure:
  image:
    name: giantswarm/capi-openstack-controller
registry:
  domain: quay.io

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

crds:
  install: true
  jobRegistry: quay.io
