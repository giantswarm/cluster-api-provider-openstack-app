tag: v0.6.0-prerelease-mario

infrastructure:
  image:
    name: giantswarm/capi-openstack-controller
registry:
  domain: quay.io

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

crds:
  install: true
  jobRegistry: quay.io
